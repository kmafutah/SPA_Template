
(function () {
    var root = this;

    //lets just run a test to make sure everything is working:
    //--------------------------------------------------------
    require(['app/test'], function (ts) {
        ts.test();
    });
    
})();